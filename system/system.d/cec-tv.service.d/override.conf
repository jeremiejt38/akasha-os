[Unit]
# Ensure the splash screen is shown before we send CEC standby
Before=splash-poweroff.service splash-reboot.service

[Service]
# The actual CEC standby is handled by splash-poweroff.service after the splash
ExecStop=
ExecStop=/bin/true
